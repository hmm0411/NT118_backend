apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cine-chart.fullname" . }}-test-connection"
  labels:
    {{- include "cine-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cine-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
