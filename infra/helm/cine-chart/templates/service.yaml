apiVersion: v1
kind: Service
metadata:
  name: {{ include "cine-chart.fullname" . }}
  labels:
    {{- include "cine-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cine-chart.selectorLabels" . | nindent 4 }}
